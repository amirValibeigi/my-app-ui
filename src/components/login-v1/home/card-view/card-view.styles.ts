import EStyleSheet from "react-native-extended-stylesheet";

export const styles = EStyleSheet.create({
  container: {
    elevation: 3,
    padding: 12,
    backgroundColor: "#efefef",
    borderRadius: 8,
  },
  title: { marginBottom: 12, fontSize: 18 },
});
